void beginGame(){

  int turnOver == 0;
  /* Begin count down */
  /* throwBall. */ 
  while(turnOver == 0){
    /* If the ball was hit, then make runner start running */
      /* If the ball was caught, then all runners are out. */
      /* Wait a few seconds, and then end the turn. */
  
    /* If the ball was not hit, Then Wait a few second, and end the turn */

  }


}
